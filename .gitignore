/target
target/
**/*.rs.bk
Cargo.lock.bak
.DS_Store
.idea/
.vscode/
*.rvf.tmp
fixtures/cache/

# Local agent + tool metadata
.claude/
.mcp.json
.openclaw/

# But DO ship our own skill folder — it's the user-facing install asset
!.claude/
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/learn-rv/
!.claude/skills/learn-rv/**

# Backup directories from refactors
crates/*.bak/
*.bak/

# Stale v0 artifacts (project is RVF-first; legacy db files do not ship)
ruvector.db
*.sqlite
*.sqlite3

# Environment — .env.example is safe to commit (no real secrets)
.env
.env.local
!.env.example

# The ruvector symlink lives as a sibling to this workspace (../ruvector -> RuVector_Clean).
# It is outside this repo so git never tracks it. See BUILDING.md.
